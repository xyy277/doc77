# Doc77 多语言化（i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Doc77 建立自研轻量 i18n 机制，将全部六层（Web UI / CLI / API 错误 / MCP & AI tool descriptions / AI system prompt / Electron 托盘）约 400-600 条中文词条提取为 zh-CN + en-US 双语词条，支持 `~/.doc77/locales/*.json` 外部语言包扩展任意语言。

**Architecture:** `packages/core/src/i18n/` 提供 `t()/initI18n()/setLocale()/getLocale()/listLocales()/resolveLocale()/getDict()`，内置 locale JSON 由 tsup 打包内联；Express 暴露 `GET /api/i18n` 下发合并词条表；前端原生 JS 用 `data-i18n` 属性 + 全局 `t()`。回退链：当前语言 → en-US → key 本身。覆盖率由 `scripts/check-i18n.sh` 扫描 `[一-龥]` 保证。

**Tech Stack:** Node.js, TypeScript, Express, tsup, vitest, 原生前端 JS（无框架无 bundler）。

**Spec:** `docs/superpowers/specs/2026-07-16-i18n-design.md`

## Global Constraints

- 每个 commit 格式 `type(scope): description`，必须以 `Co-Authored-By: xyy277 <907507646@qq.com>` 结尾（CLAUDE.md 强制）
- 每次 commit 前对 `git diff --cached` 执行隐私扫描（CLAUDE.md 的检测模式表），无命中方可提交
- 禁止发布 npm；禁止 `git reset --hard` / `git checkout -- .` 等破坏性命令
- key 命名规范：`层.模块.语义`，camelCase 语义段，如 `cli.password.tooShort`、`web.dashboard.greeting.morning`、`mcp.tool.writeFile.desc`
- 占位符语法：`{name}`，如 `"api.file.tooLarge": "文件超过 {maxSizeMB}MB 上限"`
- en-US 为基准语言（回退目标）；每次新增词条必须**同时**写入 `zh-CN.json` 和 `en-US.json`
- 词条提取后：TS/JS 中不留任何非注释中文；HTML 静态文本改为**英文原文 + `data-i18n` 属性**（英文为兜底可见内容，applyI18n 按当前语言覆盖）
- 白名单（允许保留中文）：注释行、`packages/core/src/translate/models.ts`（文档翻译模型显示名）、`packages/core/src/i18n/locales/*.json`、`__tests__`/`*.test.ts`、`docs/`
- 不改动 `packages/core/src/translate/` 的文档翻译功能逻辑
- 每个 Task 结束运行 `pnpm build && pnpm test`（156+ tests 全绿）再 commit
- 测试断言不得依赖具体语言文案：断言 `code` 字段，或在测试 setup 中 `initI18n('zh-CN')` 后用 `t()` 取期望值

---

## 词条提取通用流程（每个提取类 Task 均遵循）

1. 列出目标文件中的中文行：
   ```bash
   grep -nP '[一-龥]' <file> | grep -Ev ':[[:space:]]*(//|\*|/\*|#|--|<!--)'
   ```
2. 对每条用户可见字符串：
   - 在 `packages/core/src/i18n/locales/zh-CN.json` 中新增 key（原中文文案原样入值，动态拼接改为 `{name}` 占位符）
   - 在 `packages/core/src/i18n/locales/en-US.json` 中新增同 key 英文翻译（UI 短语用业界惯例：Save / Cancel / Approve / Settings / Sign in…）
   - 源码处替换为 `t('key')` / `t('key', { name: value })`（前端为全局 `t()`，后端 `import { t } from '@doc77/core'` 或 core 包内 `from '../i18n/index.js'`）
3. 纯中文注释保留不动；行尾中文注释保留不动（扫描脚本只拦截非注释中文）
4. 作用域校验（期望无输出）：
   ```bash
   bash scripts/check-i18n.sh <file>
   ```
5. `pnpm build && pnpm test` 全绿
6. commit

---

### Task 1: i18n 核心模块

**Files:**
- Create: `packages/core/src/i18n/index.ts`
- Create: `packages/core/src/i18n/locales/zh-CN.json`
- Create: `packages/core/src/i18n/locales/en-US.json`
- Modify: `packages/core/src/index.ts`（新增导出）
- Modify: `packages/core/src/db/config.ts`（DEFAULTS 新增 `locale.language`）
- Test: `packages/core/__tests__/i18n.test.ts`

**Interfaces:**
- Consumes: 无（零依赖模块，仅 node:fs / node:path / node:os）
- Produces（后续所有 Task 依赖）:
  - `initI18n(lang?: string, opts?: { externalDir?: string }): void`
  - `t(key: string, params?: Record<string, string | number>): string`
  - `setLocale(lang: string): void` / `getLocale(): string`
  - `listLocales(): { code: string; name: string }[]`
  - `resolveLocale(explicit?: string, hint?: string): string`
  - `getDict(lang?: string): Record<string, string>`

- [ ] **Step 1: 写失败测试**

`packages/core/__tests__/i18n.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import * as fs from 'node:fs';
import * as os from 'node:os';
import * as path from 'node:path';
import {
  initI18n, t, setLocale, getLocale, listLocales, resolveLocale, getDict,
} from '../src/i18n/index.js';

describe('i18n', () => {
  beforeEach(() => {
    initI18n('zh-CN', { externalDir: path.join(os.tmpdir(), 'doc77-no-such-dir') });
  });

  it('returns zh value when locale is zh-CN', () => {
    expect(t('test.hello')).toBe('你好');
  });

  it('falls back to en-US when key missing in current locale', () => {
    expect(t('test.enOnly')).toBe('English only');
  });

  it('falls back to key itself when missing everywhere', () => {
    expect(t('no.such.key')).toBe('no.such.key');
  });

  it('interpolates {name} params', () => {
    expect(t('test.sized', { maxSizeMB: 2 })).toBe('上限 2MB');
  });

  it('leaves unknown placeholders untouched', () => {
    expect(t('test.sized')).toBe('上限 {maxSizeMB}MB');
  });

  it('setLocale/getLocale round-trip', () => {
    setLocale('en-US');
    expect(getLocale()).toBe('en-US');
    expect(t('test.hello')).toBe('Hello');
  });

  it('resolveLocale: explicit > hint > env > en-US, zh* normalizes to zh-CN', () => {
    const oldLang = process.env.LANG;
    const oldLc = process.env.LC_ALL;
    process.env.LANG = 'en_US.UTF-8';
    delete process.env.LC_ALL;
    try {
      expect(resolveLocale('zh-CN', 'en-US')).toBe('zh-CN');
      expect(resolveLocale('', 'zh')).toBe('zh-CN');
      expect(resolveLocale('', 'zh-TW')).toBe('zh-CN');
      expect(resolveLocale('', 'fr-FR')).toBe('en-US'); // 未安装的语言落到 env → en-US
      process.env.LANG = 'zh_CN.UTF-8';
      expect(resolveLocale('', '')).toBe('zh-CN'); // env 检测
    } finally {
      process.env.LANG = oldLang;
      if (oldLc !== undefined) process.env.LC_ALL = oldLc;
    }
  });

  it('merges external pack: same-code overrides builtin, new code registers', () => {
    const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'doc77-loc-'));
    fs.writeFileSync(path.join(dir, 'zh-CN.json'),
      JSON.stringify({ _meta: { code: 'zh-CN', name: '简体中文' }, 'test.hello': '您好' }));
    fs.writeFileSync(path.join(dir, 'ja-JP.json'),
      JSON.stringify({ _meta: { code: 'ja-JP', name: '日本語' }, 'test.hello': 'こんにちは' }));
    initI18n('zh-CN', { externalDir: dir });
    expect(t('test.hello')).toBe('您好'); // 外部覆盖内置
    setLocale('ja-JP');
    expect(t('test.hello')).toBe('こんにちは');
    expect(t('test.enOnly')).toBe('English only'); // 外部语言缺 key 回退 en
    expect(listLocales().map((l) => l.code)).toContain('ja-JP');
  });

  it('getDict returns merged flat dict without _meta', () => {
    const d = getDict('zh-CN');
    expect(d['test.hello']).toBe('你好');
    expect(d['_meta']).toBeUndefined();
  });

  it('ignores malformed external json without crashing', () => {
    const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'doc77-loc-'));
    fs.writeFileSync(path.join(dir, 'bad.json'), '{ not json');
    expect(() => initI18n('zh-CN', { externalDir: dir })).not.toThrow();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run packages/core/__tests__/i18n.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建种子 locale 文件**

`packages/core/src/i18n/locales/zh-CN.json`：

```json
{
  "_meta": { "code": "zh-CN", "name": "简体中文" },
  "test.hello": "你好",
  "test.sized": "上限 {maxSizeMB}MB"
}
```

`packages/core/src/i18n/locales/en-US.json`：

```json
{
  "_meta": { "code": "en-US", "name": "English" },
  "test.hello": "Hello",
  "test.enOnly": "English only",
  "test.sized": "Limit {maxSizeMB}MB"
}
```

（`test.*` 词条供单测使用，永久保留；`test.enOnly` 故意只存在于 en——key 完整性脚本 Task 3 会为 `test.*` 前缀留白名单。）

- [ ] **Step 4: 实现 `packages/core/src/i18n/index.ts`**

```ts
/**
 * Doc77 i18n — 轻量多语言模块（零第三方依赖）
 *
 * 回退链：当前语言 → en-US → key 本身。
 * 外部语言包：~/.doc77/locales/<lang>.json，与内置同码时外部覆盖内置同名 key。
 */
import * as fs from 'node:fs';
import * as path from 'node:path';
import * as os from 'node:os';
import zhCN from './locales/zh-CN.json';
import enUS from './locales/en-US.json';

type Dict = Record<string, string>;
export interface LocaleInfo {
  code: string;
  name: string;
}

const FALLBACK = 'en-US';
const DEFAULT_EXTERNAL_DIR = path.join(os.homedir(), '.doc77', 'locales');

let dicts: Record<string, Dict> = {};
let locales: LocaleInfo[] = [];
let current = FALLBACK;

/** 从原始 JSON 对象拆出 _meta 与词条 */
function splitPack(raw: Record<string, unknown>): { meta: LocaleInfo | null; dict: Dict } {
  const dict: Dict = {};
  let meta: LocaleInfo | null = null;
  for (const [k, v] of Object.entries(raw)) {
    if (k === '_meta' && v && typeof v === 'object') {
      const m = v as Record<string, unknown>;
      if (typeof m.code === 'string' && typeof m.name === 'string') {
        meta = { code: m.code, name: m.name };
      }
    } else if (typeof v === 'string') {
      dict[k] = v;
    }
  }
  return { meta, dict };
}

/**
 * 初始化：加载内置词条，扫描外部语言包目录并合并，然后按 resolveLocale 设定当前语言。
 * 可重复调用（幂等重建）。
 */
export function initI18n(lang?: string, opts?: { externalDir?: string }): void {
  dicts = {};
  locales = [];
  for (const raw of [enUS, zhCN] as Record<string, unknown>[]) {
    const { meta, dict } = splitPack(raw);
    if (meta) {
      dicts[meta.code] = dict;
      locales.push(meta);
    }
  }
  const dir = opts?.externalDir ?? DEFAULT_EXTERNAL_DIR;
  try {
    for (const f of fs.readdirSync(dir)) {
      if (!f.endsWith('.json')) continue;
      try {
        const raw = JSON.parse(fs.readFileSync(path.join(dir, f), 'utf8')) as Record<
          string,
          unknown
        >;
        const { meta, dict } = splitPack(raw);
        if (!meta) continue;
        if (dicts[meta.code]) {
          Object.assign(dicts[meta.code], dict); // 外部覆盖内置同名 key
        } else {
          dicts[meta.code] = dict;
          locales.push(meta);
        }
      } catch {
        // 忽略损坏的语言包文件
      }
    }
  } catch {
    // 目录不存在：正常，跳过
  }
  current = resolveLocale(lang ?? '');
}

/** 语言码归一化：zh* → zh-CN；命中已加载语言用之；否则 en-US */
function normalize(code: string): string {
  if (!code) return '';
  if (dicts[code]) return code;
  if (/^zh/i.test(code)) return dicts['zh-CN'] ? 'zh-CN' : FALLBACK;
  const base = code.split(/[-_]/)[0].toLowerCase();
  const hit = Object.keys(dicts).find((c) => c.toLowerCase().startsWith(base));
  return hit ?? '';
}

/** 解析优先级：explicit（非空）> hint > 系统 LANG/LC_ALL > en-US */
export function resolveLocale(explicit?: string, hint?: string): string {
  for (const cand of [explicit, hint, process.env.LC_ALL, process.env.LANG]) {
    if (!cand) continue;
    const n = normalize(cand.split('.')[0].replace('_', '-'));
    if (n) return n;
  }
  return FALLBACK;
}

export function t(key: string, params?: Record<string, string | number>): string {
  const v = dicts[current]?.[key] ?? dicts[FALLBACK]?.[key] ?? key;
  if (!params) return v;
  return v.replace(/\{(\w+)\}/g, (m, name: string) =>
    name in params ? String(params[name]) : m,
  );
}

export function setLocale(lang: string): void {
  current = normalize(lang) || FALLBACK;
}

export function getLocale(): string {
  return current;
}

export function listLocales(): LocaleInfo[] {
  return locales.slice();
}

/** 返回某语言合并后的完整词条表（含 en 回退补齐），供 /api/i18n 下发 */
export function getDict(lang?: string): Dict {
  const code = lang ? normalize(lang) || FALLBACK : current;
  return { ...dicts[FALLBACK], ...dicts[code] };
}
```

- [ ] **Step 5: core 导出 + config 默认值**

`packages/core/src/index.ts` 在 `// Database` 段之前加：

```ts
// i18n
export {
  initI18n,
  t,
  setLocale,
  getLocale,
  listLocales,
  resolveLocale,
  getDict,
} from './i18n/index.js';
export type { LocaleInfo } from './i18n/index.js';
```

`packages/core/src/db/config.ts` 的 `DEFAULTS` 末尾加：

```ts
  'locale.language': '', // 空值 = 自动检测（系统 LANG / 浏览器 hint）
```

- [ ] **Step 6: 跑测试确认通过**

Run: `pnpm vitest run packages/core/__tests__/i18n.test.ts`
Expected: PASS（全部 10 个用例）

- [ ] **Step 7: 全量构建 + 测试 + commit**

```bash
pnpm build && pnpm test
git add packages/core/src/i18n packages/core/src/index.ts packages/core/src/db/config.ts packages/core/__tests__/i18n.test.ts
git commit -m "feat(core): add lightweight i18n module with external locale packs

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 2: `/api/i18n` endpoint 与 server 初始化

**Files:**
- Modify: `packages/core/src/server/app.ts`（`createApp()` 内）
- Test: `packages/core/__tests__/i18n-api.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `initI18n / resolveLocale / getDict / listLocales / getLocale`；`getConfig('locale.language')`
- Produces: `GET /api/i18n?lang=<code>&hint=<navigator.language>` → `{ lang, dict, available, global }`（前端 Task 4 依赖此响应结构）；`createApp()` 启动时完成 `initI18n(getConfig('locale.language'))`

- [ ] **Step 1: 写失败测试**

`packages/core/__tests__/i18n-api.test.ts`（不依赖 supertest，直接测路由处理逻辑所用的纯函数组合；endpoint 装配由 Step 3 的手工 curl 验证兜底）：

```ts
import { describe, it, expect } from 'vitest';
import { buildI18nResponse } from '../src/server/i18n-route.js';
import { initI18n } from '../src/i18n/index.js';

describe('buildI18nResponse', () => {
  it('explicit lang wins over global and hint', () => {
    initI18n('');
    const r = buildI18nResponse({ lang: 'zh-CN', hint: 'en-US', global: 'en-US' });
    expect(r.lang).toBe('zh-CN');
    expect(r.dict['test.hello']).toBe('你好');
    expect(r.global).toBe('en-US');
    expect(r.available.some((l) => l.code === 'en-US')).toBe(true);
  });

  it('falls back: no lang → global → hint', () => {
    initI18n('');
    expect(buildI18nResponse({ lang: '', hint: 'zh', global: '' }).lang).toBe('zh-CN');
    expect(buildI18nResponse({ lang: '', hint: '', global: 'en-US' }).lang).toBe('en-US');
  });

  it('etag is stable for same dict and changes across langs', () => {
    initI18n('');
    const a = buildI18nResponse({ lang: 'zh-CN', hint: '', global: '' });
    const b = buildI18nResponse({ lang: 'zh-CN', hint: '', global: '' });
    const c = buildI18nResponse({ lang: 'en-US', hint: '', global: '' });
    expect(a.etag).toBe(b.etag);
    expect(a.etag).not.toBe(c.etag);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run packages/core/__tests__/i18n-api.test.ts`
Expected: FAIL（`i18n-route.js` 不存在）

- [ ] **Step 3: 实现路由模块并挂载**

Create `packages/core/src/server/i18n-route.ts`：

```ts
/**
 * /api/i18n 路由的纯逻辑 —— 语言解析 + 词条表 + ETag。
 */
import { createHash } from 'node:crypto';
import { resolveLocale, getDict, listLocales, type LocaleInfo } from '../i18n/index.js';

export interface I18nResponse {
  lang: string;
  dict: Record<string, string>;
  available: LocaleInfo[];
  global: string;
  etag: string;
}

export function buildI18nResponse(q: {
  lang: string;
  hint: string;
  global: string;
}): I18nResponse {
  const lang = resolveLocale(q.lang || q.global, q.hint);
  const dict = getDict(lang);
  const etag = '"' + createHash('sha1').update(lang + JSON.stringify(dict)).digest('hex') + '"';
  return { lang, dict, available: listLocales(), global: q.global, etag };
}
```

`packages/core/src/server/app.ts`：

1. 顶部 import 区加：

```ts
import { initI18n } from '../i18n/index.js';
import { buildI18nResponse } from './i18n-route.js';
```

2. `createApp()` 函数体开头（现有中间件装配之前）加：

```ts
  // i18n：按全局配置初始化（config 为空 = 自动检测系统语言）
  initI18n(getConfig('locale.language') || '');
```

（`getConfig` 已在 app.ts 的现有 import 中；若无则从 `../db/config.js` 引入。）

3. 在 `app.get('/api/server-info', ...)` 之前加路由：

```ts
  // i18n 词条表下发（带 ETag 缓存）
  app.get('/api/i18n', (req: Request, res: Response) => {
    const r = buildI18nResponse({
      lang: String(req.query.lang || ''),
      hint: String(req.query.hint || ''),
      global: getConfig('locale.language') || '',
    });
    if (req.headers['if-none-match'] === r.etag) {
      res.status(304).end();
      return;
    }
    res.set('ETag', r.etag);
    res.json({ lang: r.lang, dict: r.dict, available: r.available, global: r.global });
  });
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm vitest run packages/core/__tests__/i18n-api.test.ts`
Expected: PASS

- [ ] **Step 5: 端到端手工验证**

```bash
pnpm dev:restart
sleep 2
curl -s 'http://127.0.0.1:2777/api/i18n?lang=zh-CN' | head -c 300
curl -s 'http://127.0.0.1:2777/api/i18n?hint=en-US' | head -c 300
```

Expected: 两次分别返回 `"lang":"zh-CN"` / `"lang":"en-US"` 且带 `dict`、`available`。

- [ ] **Step 6: 全量测试 + commit**

```bash
pnpm build && pnpm test
git add packages/core/src/server/i18n-route.ts packages/core/src/server/app.ts packages/core/__tests__/i18n-api.test.ts
git commit -m "feat(core): serve merged i18n dict via /api/i18n with ETag

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 3: 覆盖率扫描脚本 `check-i18n`

**Files:**
- Create: `scripts/check-i18n.sh`
- Create: `scripts/check-i18n-keys.mjs`
- Modify: `package.json`（根，scripts 加 `check:i18n`）

**Interfaces:**
- Consumes: Task 1 的 locale 文件路径
- Produces: `pnpm check:i18n`（全仓扫描）与 `bash scripts/check-i18n.sh <path...>`（作用域扫描）——后续每个提取 Task 的验收命令

- [ ] **Step 1: 写脚本**

`scripts/check-i18n.sh`：

```bash
#!/usr/bin/env bash
# i18n 覆盖率检查：
#  1) packages/*/src 内不得存在非注释硬编码中文（白名单除外）
#  2) zh-CN.json 与 en-US.json 的 key 集合必须一致
# 用法: bash scripts/check-i18n.sh [path...]   # 无参 = 全仓扫描 + key 校验
set -uo pipefail
cd "$(dirname "$0")/.."

WHITELIST='packages/core/src/translate/models\.ts|packages/core/src/i18n/locales/|__tests__|\.test\.ts'
TARGETS=("$@")
if [ ${#TARGETS[@]} -eq 0 ]; then
  TARGETS=(packages/*/src)
fi

FAIL=0
HITS=$(grep -rnP '[一-龥]' "${TARGETS[@]}" \
        --include='*.ts' --include='*.js' --include='*.html' 2>/dev/null \
      | grep -Ev "$WHITELIST" \
      | grep -Ev ':[0-9]+:[[:space:]]*(//|\*|/\*|#|--|<!--)' || true)
if [ -n "$HITS" ]; then
  echo "❌ 发现未提取的硬编码中文（非注释）:"
  echo "$HITS"
  FAIL=1
fi

if [ $# -eq 0 ]; then
  node scripts/check-i18n-keys.mjs || FAIL=1
fi

if [ $FAIL -eq 0 ]; then
  echo "✅ check-i18n passed"
fi
exit $FAIL
```

`scripts/check-i18n-keys.mjs`：

```js
// zh-CN 与 en-US key 集合双向 diff（test.* 为单测专用词条，允许不对称）
import { readFileSync } from 'node:fs';

const load = (p) => {
  const o = JSON.parse(readFileSync(p, 'utf8'));
  return new Set(Object.keys(o).filter((k) => k !== '_meta' && !k.startsWith('test.')));
};
const zh = load('packages/core/src/i18n/locales/zh-CN.json');
const en = load('packages/core/src/i18n/locales/en-US.json');
const onlyZh = [...zh].filter((k) => !en.has(k));
const onlyEn = [...en].filter((k) => !zh.has(k));
if (onlyZh.length || onlyEn.length) {
  if (onlyZh.length) console.error('❌ 仅存在于 zh-CN 的 key:', onlyZh);
  if (onlyEn.length) console.error('❌ 仅存在于 en-US 的 key:', onlyEn);
  process.exit(1);
}
console.log(`✅ i18n key parity OK (${zh.size} keys)`);
```

根 `package.json` 的 `scripts` 加：

```json
    "check:i18n": "bash scripts/check-i18n.sh",
```

- [ ] **Step 2: 验证脚本当前输出（预期失败 = 基线正确）**

```bash
chmod +x scripts/check-i18n.sh
pnpm check:i18n; echo "exit=$?"
```

Expected: 列出约 28 个文件的中文命中，`exit=1`。人工核对输出文件清单与摸底调查一致（cli/doc77.ts、core/server/*.ts、core/web/**、mcp/server.ts、ai/tools.ts、ai/agent/index.ts、electron/tray.ts 等）。**将此输出保存为提取进度清单的基线。**

同时验证 key 校验单独可用：`node scripts/check-i18n-keys.mjs` → `✅ i18n key parity OK (1 keys)`（目前只有 `test.sized`；`test.*` 前缀被排除后 zh 侧剩 0-1 个正式 key，输出数字以实际为准，不报错即可）。

- [ ] **Step 3: Commit**

```bash
git add scripts/check-i18n.sh scripts/check-i18n-keys.mjs package.json
git commit -m "chore(scripts): add check-i18n coverage scanner and key parity check

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 4: 前端 i18n runtime（桌面 + mobile）

**Files:**
- Modify: `packages/core/src/web/js/common.js`（文件顶部）
- Modify: `packages/core/src/web/mobile/js/common.js`（文件顶部，同样代码）
- Modify: `packages/core/src/web/index.html`、`preview.html`、`guide.html`、`mobile/index.html`、`mobile/preview.html`（`<head>` 防闪烁片段）

**Interfaces:**
- Consumes: Task 2 的 `GET /api/i18n` 响应 `{ lang, dict, available, global }`
- Produces（Task 12-16 依赖）:
  - 全局 `t(key, params)` 与 `applyI18n(root?)`
  - `window.__doc77_i18n_ready`（Promise，词条加载完成）
  - `window.__doc77_lang / __doc77_locales / __doc77_lang_global`
  - localStorage key：`doc77_lang`（值为语言码；不存在 = 跟随全局）

- [ ] **Step 1: 在两个 common.js 文件顶部（现有 capabilities fetch 之前）插入 runtime**

```js
//══════════ i18n ══════════
window.__doc77_dict = {};
window.t = function (key, params) {
  var v = window.__doc77_dict[key] || key;
  return v.replace(/\{(\w+)\}/g, function (m, name) {
    return params && name in params ? String(params[name]) : m;
  });
};
window.applyI18n = function (root) {
  root = root || document;
  root.querySelectorAll('[data-i18n]').forEach(function (el) {
    el.textContent = t(el.getAttribute('data-i18n'));
  });
  root.querySelectorAll('[data-i18n-title]').forEach(function (el) {
    el.title = t(el.getAttribute('data-i18n-title'));
  });
  root.querySelectorAll('[data-i18n-placeholder]').forEach(function (el) {
    el.placeholder = t(el.getAttribute('data-i18n-placeholder'));
  });
};
window.__doc77_i18n_ready = fetch('/api/i18n?' + (function () {
  var o = localStorage.getItem('doc77_lang');
  return o ? 'lang=' + encodeURIComponent(o)
           : 'hint=' + encodeURIComponent(navigator.language || '');
})()).then(function (r) { return r.json(); }).then(function (d) {
  window.__doc77_dict = d.dict;
  window.__doc77_lang = d.lang;
  window.__doc77_locales = d.available;
  window.__doc77_lang_global = d.global;
  document.documentElement.lang = d.lang;
}).catch(function () {}).then(function () {
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', function () { applyI18n(); });
  } else {
    applyI18n();
  }
  document.documentElement.classList.remove('i18n-loading');
});
```

- [ ] **Step 2: 五个 HTML 的 `<head>` 最前加防闪烁片段**

```html
<script>document.documentElement.classList.add('i18n-loading');setTimeout(function(){document.documentElement.classList.remove('i18n-loading')},1500);</script>
<style>.i18n-loading body{visibility:hidden}</style>
```

（1500ms 超时兜底：API 失败也不会白屏卡死。）

- [ ] **Step 3: 设置面板加语言选项**

`packages/core/src/web/js/common.js` 设置面板生成处（`'<div class="section-title">...'` 系列、System/系统分区，约 L280-310 附近；用 `grep -n "section-title" packages/core/src/web/js/common.js` 定位）加两行设置：

```js
    // 语言（本浏览器覆盖 + 全局）
    '<div class="setting-row"><label data-i18n="common.settings.uiLang">Interface language (this browser)</label>' +
      langSelect('uiLangSelect') + '</div>' +
    '<div class="setting-row"><label data-i18n="common.settings.globalLang">Global language (server / AI / CLI)</label>' +
      langSelect('globalLangSelect') + '</div>' +
```

并新增两个函数（settingRow 等 helper 同级位置）：

```js
function langSelect(id) {
  var opts = '<option value="">' + t('common.settings.followGlobal') + '</option>';
  (window.__doc77_locales || []).forEach(function (l) {
    opts += '<option value="' + l.code + '">' + l.name + '</option>';
  });
  return '<select id="' + id + '" onchange="onLangChange(this)">' + opts + '</select>';
}
function onLangChange(sel) {
  if (sel.id === 'uiLangSelect') {
    if (sel.value) localStorage.setItem('doc77_lang', sel.value);
    else localStorage.removeItem('doc77_lang');
    location.reload();
  } else {
    // 全局语言写 config；MCP tool descriptions 需重启服务生效
    fetch('/api/config', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ 'locale.language': sel.value }),
    }).then(function () { showToast(t('common.settings.globalLangSaved')); });
  }
}
```

注意：设置面板打开时用 `__doc77_lang / __doc77_lang_global` 回填两个 select 的当前值；`/api/config` 若与现有设置保存端点路径不同（用 `grep -n "api/config\|saveSettings" packages/core/src/web/js/common.js` 确认实际保存端点），改用现有端点的调用方式。全局语言下拉的第一个 option 文案用 `common.settings.autoDetect`（"自动检测"/"Auto-detect"）而非 followGlobal。

本 Task 新增词条（同时写入 zh-CN.json / en-US.json）：

```
common.settings.uiLang        界面语言（本浏览器）        Interface language (this browser)
common.settings.globalLang    全局语言（服务器/AI/CLI）   Global language (server / AI / CLI)
common.settings.followGlobal  跟随全局                    Follow global
common.settings.autoDetect    自动检测                    Auto-detect
common.settings.globalLangSaved  已保存，MCP 工具描述需重启服务生效  Saved. Restart the service for MCP tool descriptions to take effect
```

- [ ] **Step 4: 手工验证**

```bash
pnpm dev:restart
```

浏览器打开 `http://127.0.0.1:2777`：控制台执行 `t('test.hello')` 返回「你好」；`localStorage.setItem('doc77_lang','en-US'); location.reload()` 后返回 `Hello`；设置面板出现两个语言下拉。移动端 `http://127.0.0.1:2777/mobile/` 同样验证 `t('test.hello')`。

- [ ] **Step 5: 全量测试 + commit**

```bash
pnpm build && pnpm test
git add packages/core/src/web packages/core/src/i18n/locales
git commit -m "feat(web): add frontend i18n runtime with data-i18n and language settings

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 5: 提取 core 后端 — `crypto.ts` + `server/auth.ts`（含 error code）

**Files:**
- Modify: `packages/core/src/crypto.ts`（4 处密码强度反馈）
- Modify: `packages/core/src/server/auth.ts`（8 处）
- Modify: `packages/core/src/i18n/locales/zh-CN.json` / `en-US.json`
- Test: `packages/core/__tests__/auth.test.ts`（改 code 断言）

**Interfaces:**
- Consumes: `t()`（core 包内 `import { t } from '../i18n/index.js'`，crypto.ts 用 `./i18n/index.js`）
- Produces: auth 错误响应对象增加稳定 `code` 字段（string，SCREAMING_SNAKE），如 `{ error: t('api.auth.wrongPassword', { fails }), code: 'WRONG_PASSWORD' }`。后续 app.ts（Task 6）沿用同一 code 模式。

- [ ] **Step 1: 列出中文行**

```bash
grep -nP '[一-龥]' packages/core/src/crypto.ts packages/core/src/server/auth.ts | grep -Ev ':[0-9]+:[[:space:]]*(//|\*|/\*|#)'
```

- [ ] **Step 2: 按通用流程提取**

key 前缀：`api.password.*`（crypto 强度反馈）、`api.auth.*`（auth 流程）。替换示例（auth.ts）：

```ts
// Before
return { ok: false, error: `密码错误（${fails}/5）` };
// After
return { ok: false, error: t('api.auth.wrongPassword', { fails, max: 5 }), code: 'WRONG_PASSWORD' };
```

对应词条：

```json
"api.auth.wrongPassword": "密码错误（{fails}/{max}）"
"api.auth.wrongPassword": "Incorrect password ({fails}/{max})"
```

auth.ts 中每个含 `error:` 的返回都补 `code` 字段（`NO_PASSWORD` / `ACCOUNT_LOCKED` / `PASSWORD_RESET_REQUIRED` / `TOO_MANY_ATTEMPTS` / `WRONG_PASSWORD` / `NO_RECOVERY_CODE` 按语义命名）。crypto.ts 的 `checkPasswordStrength` 返回的 feedback 数组仅替换文案为 `t()`，不需要 code。

- [ ] **Step 3: 更新测试**

`packages/core/__tests__/auth.test.ts:79` 的

```ts
expect(result.error).toMatch(/密码错误/);
```

改为：

```ts
expect(result.code).toBe('WRONG_PASSWORD');
```

同文件如有其他中文断言一并改 code。测试文件顶部若无 i18n 初始化则加：

```ts
import { initI18n } from '../src/i18n/index.js';
beforeAll(() => initI18n('zh-CN'));
```

- [ ] **Step 4: 作用域校验 + 测试**

```bash
bash scripts/check-i18n.sh packages/core/src/crypto.ts packages/core/src/server/auth.ts
pnpm vitest run packages/core/__tests__/auth.test.ts
pnpm build && pnpm test
```

Expected: 扫描 `✅ check-i18n passed`；测试全绿。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/crypto.ts packages/core/src/server/auth.ts packages/core/src/i18n/locales packages/core/__tests__/auth.test.ts
git commit -m "refactor(core): externalize auth and password-strength strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 6: 提取 core 后端 — `server/app.ts`（33 处 + error code）

**Files:**
- Modify: `packages/core/src/server/app.ts`
- Modify: locale 双文件

**Interfaces:**
- Consumes: `t()`；Task 5 确立的 `{ error, code }` 响应模式
- Produces: 所有 `res.json({ error: ... })` 中文错误变为 `{ error: t('api.…'), code: '…' }`；前端展示逻辑不变（仍读 `error` 字段），Task 12-14 中前端如有对中文报错文案的字符串匹配需改为匹配 `code`

- [ ] **Step 1: 列出中文行**

```bash
grep -nP '[一-龥]' packages/core/src/server/app.ts | grep -Ev ':[0-9]+:[[:space:]]*(//|\*)'
```

- [ ] **Step 2: 按通用流程提取（key 前缀 `api.*`，按路由语义分组）**

替换示例：

```ts
// Before
res.status(403).json({ error: '此文件类型不可编辑' });
// After
res.status(403).json({ error: t('api.edit.typeNotEditable'), code: 'EDIT_TYPE_NOT_ALLOWED' });

// Before
res.status(413).json({ error: `文件超过 ${maxSizeMB}MB 上限` });
// After
res.status(413).json({ error: t('api.file.tooLarge', { maxSizeMB }), code: 'FILE_TOO_LARGE' });
```

特别注意 app.ts 中发给 AI 的项目上下文/指令中文文本（`/api/ai/*` 路由内）：同样提取为 `ai.context.*` 词条，使 AI 收到的语境与全局语言一致。

- [ ] **Step 3: 作用域校验 + 手工验证 + 测试**

```bash
bash scripts/check-i18n.sh packages/core/src/server/app.ts
pnpm build && pnpm test
pnpm dev:restart && sleep 2
curl -s -X POST http://127.0.0.1:2777/api/projects -H 'Content-Type: application/json' -d '{}' | head -c 200
```

Expected: 扫描通过；curl 返回的 error 为当前全局语言文案且带 `code` 字段。

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/server/app.ts packages/core/src/i18n/locales
git commit -m "refactor(core): externalize app.ts API error messages to i18n with codes

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 7: 提取 core 后端 — `ai-tools.ts` / `dialog.ts` / `vendor.ts` / renderers / `index.ts`

**Files:**
- Modify: `packages/core/src/server/ai-tools.ts`（7 处，AI 运行时文本）
- Modify: `packages/core/src/server/dialog.ts`、`packages/core/src/server/vendor.ts`
- Modify: `packages/core/src/renderers/markdown.ts`、`packages/core/src/renderers/pdf.ts`
- Modify: `packages/core/src/index.ts`（如有非注释中文）
- Test: `packages/core/__tests__/ai-tools.test.ts`

**Interfaces:**
- Consumes: `t()`
- Produces: ai-tools 返回给 AI 的文本词条 `ai.runtime.*`（如 `ai.runtime.queuedForApproval`、`ai.runtime.sensitiveRejected`、`ai.runtime.riskLevelDenied`）

- [ ] **Step 1: 列出中文行**（五个文件一起）

```bash
grep -nP '[一-龥]' packages/core/src/server/ai-tools.ts packages/core/src/server/dialog.ts packages/core/src/server/vendor.ts packages/core/src/renderers/markdown.ts packages/core/src/renderers/pdf.ts packages/core/src/index.ts | grep -Ev ':[0-9]+:[[:space:]]*(//|\*|/\*)'
```

- [ ] **Step 2: 按通用流程提取**

替换示例（ai-tools.ts）：

```ts
// Before
return `[待审批] 已加入审批队列，任务ID: ${task.task_id}（${desc}）。请前往「审批」标签页查看并批准或拒绝。`;
// After
return t('ai.runtime.queuedForApproval', { taskId: task.task_id, desc });
```

```json
"ai.runtime.queuedForApproval": "[待审批] 已加入审批队列，任务ID: {taskId}（{desc}）。请前往「审批」标签页查看并批准或拒绝。"
"ai.runtime.queuedForApproval": "[Pending approval] Queued for approval, task ID: {taskId} ({desc}). Go to the Approvals tab to approve or reject."
```

renderers 中若中文是渲染失败等用户可见提示 → 提取为 `api.render.*`；若是注入到预览 HTML 的界面文字 → 提取为 `web.preview.*`。

- [ ] **Step 3: 更新 `ai-tools.test.ts`**

文件顶部加：

```ts
import { initI18n } from '../src/i18n/index.js';
beforeAll(() => initI18n('zh-CN'));
```

原有 `expect(res).toMatch(/审批/)` 等正则在 zh-CN 下仍然命中，无需改断言本体（`/敏感|sensitive/i`、`/风险|不允许|risk/i` 双语正则天然兼容）。

- [ ] **Step 4: 校验 + 测试 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/server/ai-tools.ts packages/core/src/server/dialog.ts packages/core/src/server/vendor.ts packages/core/src/renderers packages/core/src/index.ts
pnpm build && pnpm test
git add packages/core/src packages/core/__tests__/ai-tools.test.ts
git commit -m "refactor(core): externalize ai-tools, dialog, vendor and renderer strings

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 8: 提取 mcp — tool descriptions + 入口初始化

**Files:**
- Modify: `packages/mcp/src/server.ts`（9 个 tool × description + `.describe()` 参数，共 28 处）
- Modify: `packages/mcp/src/index.ts`（MCP 启动入口，`createMcpServer()` 调用方；用 `grep -n "createMcpServer" packages/mcp/src packages/cli/src -r` 找到全部启动路径）
- Modify: locale 双文件
- Test: `packages/mcp/__tests__/ai-write-integration.test.ts`

**Interfaces:**
- Consumes: `t()`、`initI18n()`、`getConfig('locale.language')`（均来自 `@doc77/core`）
- Produces: `createMcpServer()` 语义不变；descriptions 语言 = 调用时的全局语言（启动时固定，重启生效——与 spec §4 一致）

- [ ] **Step 1: 在所有 MCP 启动入口（stdio / http），`createMcpServer()` 之前确保初始化**

```ts
import { initDatabase, runMigrations, loadDefaults, getConfig, initI18n } from '@doc77/core';
// 现有 DB 初始化之后：
initI18n(getConfig('locale.language') || '');
```

若入口已由 CLI 统一初始化（CLI 在 Task 10 中 initI18n），则此处只需保证独立启动路径（如直接 `node packages/mcp/dist/...`）也初始化；没有独立启动路径则跳过本步。

- [ ] **Step 2: 提取 descriptions（key 前缀 `mcp.tool.<toolName>.desc` / `mcp.param.*`）**

替换示例：

```ts
// Before
description: '创建或覆盖文件内容（需审批）',
inputSchema: {
  project_id: z.number().describe('项目 ID'),
  file_path: z.string().describe('文件路径（相对于项目根目录）'),
},
// After
description: t('mcp.tool.writeFile.desc'),
inputSchema: {
  project_id: z.number().describe(t('mcp.param.projectId')),
  file_path: z.string().describe(t('mcp.param.filePath')),
},
```

参数描述词条跨 tool 复用（`mcp.param.projectId` / `mcp.param.dirPath` / `mcp.param.filePath` 等只建一次）。

```json
"mcp.tool.writeFile.desc": "创建或覆盖文件内容（需审批）"
"mcp.tool.writeFile.desc": "Create or overwrite file content (requires approval)"
"mcp.param.projectId": "项目 ID"
"mcp.param.projectId": "Project ID"
```

- [ ] **Step 3: 更新测试**

`packages/mcp/__tests__/ai-write-integration.test.ts` 顶部加 `initI18n('zh-CN')`（beforeAll），原 `/待审批/`、`/敏感|sensitive/i` 断言保持不变。

- [ ] **Step 4: 校验 + 测试 + commit**

```bash
bash scripts/check-i18n.sh packages/mcp/src
pnpm build && pnpm test
git add packages/mcp/src packages/mcp/__tests__ packages/core/src/i18n/locales
git commit -m "refactor(mcp): externalize tool descriptions to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 9: 提取 ai — tools 工厂化 + SYSTEM_PROMPT + classify

**Files:**
- Modify: `packages/ai/src/tools.ts`（模块级常量 → 工厂函数，19 处 description）
- Modify: `packages/ai/src/agent/index.ts`（SYSTEM_PROMPT、createClassifyPrompt）
- Modify: `packages/ai/src/index.ts` 及全部 `READ_TOOLS` / `WRITE_TOOLS` 引用点（`grep -rn "READ_TOOLS\|WRITE_TOOLS" packages --include='*.ts' | grep -v dist | grep -v __tests__`，已知：`packages/cli/src/bin/doc77.ts`、`packages/core/src/server/ai-tools.ts`、`packages/core/src/server/app.ts`）
- Test: `packages/ai/__tests__/tools.test.ts`

**Interfaces:**
- Consumes: `t()`、`getLocale()`（from `@doc77/core`）
- Produces:
  - `getReadTools(): ToolDefinition[]` / `getWriteTools(): ToolDefinition[]`（替代 `READ_TOOLS` / `WRITE_TOOLS` 常量——常量在模块加载时求值，早于 initI18n，必须工厂化）
  - `DocAgent` 构造时按当前语言取 `t('ai.systemPrompt')`

- [ ] **Step 1: tools.ts 工厂化 + 提取**

```ts
// Before
export const READ_TOOLS: ToolDefinition[] = [ { ...description: '列出项目指定目录下…' } ];
// After
export function getReadTools(): ToolDefinition[] {
  return [
    {
      type: 'function',
      function: {
        name: 'list_files',
        description: t('ai.tool.listFiles.desc'),
        parameters: {
          type: 'object',
          properties: {
            dir_path: { type: 'string', description: t('ai.tool.listFiles.dirPath') },
          },
          required: ['dir_path'],
        },
      },
    },
    // …其余 tool 同理
  ];
}
```

`WRITE_TOOLS` → `getWriteTools()` 同理。更新所有引用点（含 `packages/ai/src/index.ts` 的 re-export）。core 侧引用若通过动态 `import('@doc77/ai')`，同样把 `mod.READ_TOOLS` 改为 `mod.getReadTools()`。

- [ ] **Step 2: SYSTEM_PROMPT 词条化**

`packages/ai/src/agent/index.ts`：删除模块级 `SYSTEM_PROMPT`，构造函数改为：

```ts
this.messages = [{ role: 'system', content: t('ai.systemPrompt') }];
```

zh-CN.json 的 `ai.systemPrompt` = 现有完整中文 prompt（原文照搬，JSON 内用 `\n` 转义换行），其中第 4 条原则「**中文优先** — 始终使用中文回复」保持；en-US.json 的 `ai.systemPrompt` = 完整英文重写版（整体翻译，不逐句拼接），第 4 条为 `**Consistent language** — Always reply in English`。`createClassifyPrompt` 的模板同样提取为 `ai.classifyPrompt`（含 `{fileList}` 占位符）。

- [ ] **Step 3: 更新测试**

`packages/ai/__tests__/tools.test.ts`：`beforeAll(() => initI18n('zh-CN'))`；`READ_TOOLS`/`WRITE_TOOLS` 引用改为 `getReadTools()`/`getWriteTools()`；`expect(t.function.description).toMatch(/审批/)` 断言保留（zh-CN 下命中）。`live-e2e.test.ts` 若引用常量同样改工厂调用，测试消息保持中文并 `initI18n('zh-CN')`。

- [ ] **Step 4: 校验 + 测试 + commit**

```bash
bash scripts/check-i18n.sh packages/ai/src
pnpm build && pnpm test
git add packages/ai packages/cli/src packages/core/src packages/core/src/i18n/locales
git commit -m "refactor(ai): externalize system prompt and tool definitions to i18n factories

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 10: 提取 cli — `bin/doc77.ts`（116 行）

**Files:**
- Modify: `packages/cli/src/bin/doc77.ts`
- Modify: locale 双文件

**Interfaces:**
- Consumes: `t()`、`initI18n()`、`getConfig`（均已从 `@doc77/core` 导入）
- Produces: CLI 全部输出走 i18n；语言 = config `locale.language`，为空按系统 `LANG`

- [ ] **Step 1: 启动时初始化**

`doc77.ts` 现有初始化序列（约 L60）：

```ts
await initDatabase(DB_PATH);
runMigrations();
loadDefaults();
// 新增：
initI18n(getConfig('locale.language') || '');
```

注意：个别子命令若在 DB 初始化之前就有输出（用 `grep -n "console\." packages/cli/src/bin/doc77.ts | head -5` 核查顺序），这些早期输出行在 initI18n 之前执行时 `t()` 会返回 en-US 内置值（initI18n 未调用时 dicts 为空 → 返回 key）。处理：在文件入口最顶部先无参调用一次 `initI18n('')`（纯环境检测，不依赖 DB），DB 就绪后再按 config 重新 `initI18n(...)`。

- [ ] **Step 2: 按通用流程提取（key 前缀 `cli.*`，按子命令分组）**

替换示例：

```ts
// Before
console.error('❌ 密码至少6位');
// After
console.error(t('cli.password.tooShort'));

// Before
console.log(`📥 下载模型 ${mi.displayName} (${mi.size})...`);
// After
console.log(t('cli.translate.downloading', { name: mi.displayName, size: mi.size }));
```

```json
"cli.password.tooShort": "❌ 密码至少6位"
"cli.password.tooShort": "❌ Password must be at least 6 characters"
"cli.translate.downloading": "📥 下载模型 {name} ({size})..."
"cli.translate.downloading": "📥 Downloading model {name} ({size})..."
```

emoji 前缀保留在词条值里（两种语言一致）。

- [ ] **Step 3: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/cli/src
pnpm build && pnpm test
node packages/cli/dist/bin/doc77.js config get locale.language   # 输出正常
LANG=en_US.UTF-8 node packages/cli/dist/bin/doc77.js --help 2>&1 | head -5   # 英文输出（config 为空时）
git add packages/cli/src packages/core/src/i18n/locales
git commit -m "refactor(cli): externalize CLI output strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 11: 提取 electron — `tray.ts` + `main.ts`

**Files:**
- Modify: `packages/electron/src/tray.ts`、`packages/electron/src/main.ts`
- Modify: locale 双文件

**Interfaces:**
- Consumes: `t()`、`initI18n()`、`getConfig`（from `@doc77/core`，electron 已依赖）
- Produces: 托盘菜单按全局语言显示

- [ ] **Step 1: main.ts 在启动 server / 创建托盘之前确认 i18n 已初始化**（若 main.ts 复用 core 的 createApp 则已初始化；托盘创建若早于 createApp，需在 app.whenReady 后显式 `initI18n(getConfig('locale.language') || '')`——以 `grep -n "createApp\|createTray" packages/electron/src/main.ts` 的实际顺序为准）

- [ ] **Step 2: 提取 tray.ts**

```ts
// Before
{ label: '📁 打开 Doc77', click: () => onClick() },
{ label: '❌ 退出 Doc77', click: () => { app.quit(); } },
// After
{ label: t('electron.tray.open'), click: () => onClick() },
{ label: t('electron.tray.quit'), click: () => { app.quit(); } },
```

```json
"electron.tray.open": "📁 打开 Doc77"     /  "📁 Open Doc77"
"electron.tray.quit": "❌ 退出 Doc77"     /  "❌ Quit Doc77"
```

main.ts 中其余用户可见中文（窗口标题、对话框等）同流程提取为 `electron.*`。

- [ ] **Step 3: 校验 + commit**

```bash
bash scripts/check-i18n.sh packages/electron/src
pnpm build && pnpm test
git add packages/electron/src packages/core/src/i18n/locales
git commit -m "refactor(electron): externalize tray and main strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 12: 提取前端 — `common.js` + `mobile/js/common.js`（约 160 条）

**Files:**
- Modify: `packages/core/src/web/js/common.js`、`packages/core/src/web/mobile/js/common.js`
- Modify: locale 双文件

**Interfaces:**
- Consumes: Task 4 的全局 `t()`；`__doc77_i18n_ready`
- Produces: `common.*` 词条命名空间（toast、confirm、登录门、设置面板标签、loading 文案）；后续页面 Task 复用这些通用词条（如 `common.confirm.ok` / `common.confirm.cancel`）

- [ ] **Step 1: 列出中文行**

```bash
grep -nP '[一-龥]' packages/core/src/web/js/common.js packages/core/src/web/mobile/js/common.js | grep -Ev ':[0-9]+:[[:space:]]*(//|\*|/\*)'
```

- [ ] **Step 2: 按通用流程提取（key 前缀 `common.*`）**

替换示例：

```js
// Before
msg = msg || '请稍候...';
// After
msg = msg || t('common.loading.pleaseWait');

// Before（设置面板 label 是 JS 拼接的 HTML 字符串）
settingRow('共享密钥','security.shared_secret','password','')
// After
settingRow(t('common.settings.sharedSecret'),'security.shared_secret','password','')
```

注意时序：common.js 中**页面加载即执行**的渲染（如登录门、版本 badge title）必须包在 `__doc77_i18n_ready.then(...)` 内或在渲染函数调用前 await，否则 `t()` 返回 key。逐个检查提取点的执行时机。

- [ ] **Step 3: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/web/js/common.js packages/core/src/web/mobile/js/common.js
pnpm build && pnpm test
pnpm dev:restart
```

浏览器验证：zh / en 两种语言下 toast、confirm 弹窗、设置面板、登录界面文案均正确、无裸 key 显示。

```bash
git add packages/core/src/web packages/core/src/i18n/locales
git commit -m "refactor(web): externalize common.js shared strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 13: 提取前端 — `index.html` + dashboard/discover/favorites/stats/tabs JS

**Files:**
- Modify: `packages/core/src/web/index.html`
- Modify: `packages/core/src/web/js/dashboard.js`、`discover.js`、`favorites.js`、`stats.js`、`tabs.js`
- Modify: locale 双文件

**Interfaces:**
- Consumes: `t()` / `applyI18n()` / `data-i18n`；`common.*` 通用词条
- Produces: `web.index.*`、`web.dashboard.*`、`web.discover.*`、`web.favorites.*`、`web.stats.*`、`web.tabs.*`

- [ ] **Step 1: HTML 静态文本改造**

规则：中文文本 → **英文原文** + `data-i18n` key。示例：

```html
<!-- Before -->
<button id="registerBtn">➕ 注册项目</button>
<!-- After -->
<button id="registerBtn" data-i18n="web.index.registerProject">➕ Register project</button>
```

带 emoji 的按钮：emoji 放进词条值（`"web.index.registerProject": "➕ 注册项目"` / `"➕ Register project"`），元素内容整体替换。`title` / `placeholder` 用 `data-i18n-title` / `data-i18n-placeholder`。`<title>` 标签同样加 `data-i18n`。

- [ ] **Step 2: JS 动态字符串改造**

```js
// Before (dashboard.js)
var greeting = hour < 12 ? '早上好' : hour < 18 ? '下午好' : '晚上好';
// After
var greeting = hour < 12 ? t('web.dashboard.greeting.morning')
             : hour < 18 ? t('web.dashboard.greeting.afternoon')
             : t('web.dashboard.greeting.evening');

// Before（相对时间）
return mins + ' 分钟前';
// After
return t('web.dashboard.time.minutesAgo', { n: mins });   // "{n} 分钟前" / "{n} min ago"
```

页面初始渲染包 `__doc77_i18n_ready.then(init)`（每个 JS 的入口调用处）。

- [ ] **Step 3: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/web/index.html packages/core/src/web/js/dashboard.js packages/core/src/web/js/discover.js packages/core/src/web/js/favorites.js packages/core/src/web/js/stats.js packages/core/src/web/js/tabs.js
pnpm build && pnpm test && pnpm dev:restart
```

浏览器 zh / en 双语走查首页全部区块（英雄区、项目列表、扫描发现、收藏、统计、空状态）。

```bash
git add packages/core/src/web packages/core/src/i18n/locales
git commit -m "refactor(web): externalize index page and dashboard strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 14: 提取前端 — `preview.html` + `preview.js` + `temp-preview.js`（约 150 条）

**Files:**
- Modify: `packages/core/src/web/preview.html`、`packages/core/src/web/js/preview.js`、`packages/core/src/web/js/temp-preview.js`
- Modify: locale 双文件

**Interfaces:**
- Consumes: 同 Task 13
- Produces: `web.preview.*` 词条

- [ ] **Step 1: 改造 HTML 与 JS**（HTML 中文 → 英文原文 + `data-i18n` key；JS 动态字符串 → `t()`；`title`/`placeholder` 用 `data-i18n-title`/`data-i18n-placeholder`）

文件类型标签表替换示例（preview.js）：

```js
// Before
var labels = { video:'🎬 视频', audio:'🎵 音频', /* … */ };
// After
var labels = {
  video: t('web.preview.type.video'), audio: t('web.preview.type.audio'),
  archive: t('web.preview.type.archive'), font: t('web.preview.type.font'),
  database: t('web.preview.type.database'), design: t('web.preview.type.design'),
  binary: t('web.preview.type.binary'), gis: t('web.preview.type.gis'),
  '3d': t('web.preview.type.model3d'), ebook: t('web.preview.type.ebook'),
  document: t('web.preview.type.document'), spreadsheet: t('web.preview.type.spreadsheet'),
  presentation: t('web.preview.type.presentation'), too_large: t('web.preview.type.tooLarge'),
  unknown: t('web.preview.type.unknown'),
};
```

注意此表若为模块级立即求值，须移入函数或在 `__doc77_i18n_ready` 之后构建。审批任务状态（执行中/已执行/已拒绝）→ `web.preview.task.*`；编辑冲突按钮（保存并切换/放弃修改）→ `web.preview.edit.*`。

- [ ] **Step 2: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/web/preview.html packages/core/src/web/js/preview.js packages/core/src/web/js/temp-preview.js
pnpm build && pnpm test && pnpm dev:restart
```

浏览器 zh / en 双语走查预览页（文件树、各类型文件占位、审批队列、编辑器、AI 面板、TTS 工具栏）。

```bash
git add packages/core/src/web packages/core/src/i18n/locales
git commit -m "refactor(web): externalize preview page strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 15: 提取前端 — `guide.html`（约 200 行中文，纯静态）

**Files:**
- Modify: `packages/core/src/web/guide.html`
- Modify: locale 双文件

**Interfaces:**
- Consumes: `data-i18n` / `applyI18n()`（guide.html 需引入 common.js——用 `grep -n "common.js" packages/core/src/web/guide.html` 确认，未引入则在底部加 `<script src="/js/common.js"></script>`）
- Produces: `web.guide.*` 词条

- [ ] **Step 1: 逐区块改造**

规则：`data-i18n` 只加在**叶子文本元素**上（`<h1>`-`<h4>`、`<p>`、`<li>`、`<td>`、`<span>`、`<button>`）；若某段落内含 `<b>`/`<code>` 等内联标记，把该段拆成多个带独立 key 的 `<span>`，或将标记外移使文本节点纯净——**禁止**用 innerHTML 注入词条（外部语言包是第三方内容，innerHTML 有 XSS 风险）。key 按区块编号：`web.guide.quickstart.title`、`web.guide.quickstart.step1` ……

- [ ] **Step 2: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/web/guide.html
pnpm build && pnpm test && pnpm dev:restart
```

浏览器 zh / en 双语通读 guide 页全文，确认无裸 key、无残留单语段落。

```bash
git add packages/core/src/web/guide.html packages/core/src/i18n/locales
git commit -m "refactor(web): externalize guide page strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 16: 提取前端 — mobile 两页 + mobile JS 残余

**Files:**
- Modify: `packages/core/src/web/mobile/index.html`、`packages/core/src/web/mobile/preview.html`
- Modify: `packages/core/src/web/mobile/js/` 下所有含中文的 JS（Task 12 已处理 mobile common.js，本 Task 收尾其余）
- Modify: locale 双文件

**Interfaces:**
- Consumes: 同 Task 13；mobile 设置页复用 Task 4 的语言下拉
- Produces: `web.mobile.*` 词条（与桌面语义相同的文案**复用桌面 key**，仅 mobile 独有的用 `web.mobile.*`）

- [ ] **Step 1: 改造两页 HTML 与残余 JS**

规则与通用流程一致：HTML 中文 → **英文原文 + `data-i18n` key**；JS 动态字符串 → `t()`；初始渲染包 `__doc77_i18n_ready.then(...)`。示例：

```html
<!-- Before (mobile/index.html) -->
<div class="section-title">最近浏览</div>
<div class="empty-hint">暂无浏览记录</div>
<!-- After -->
<div class="section-title" data-i18n="web.mobile.recentFiles">Recently viewed</div>
<div class="empty-hint" data-i18n="web.mobile.noRecent">No browsing history yet</div>
```

```js
// Before (mobile js)
el.textContent = '加载中...';
// After
el.textContent = t('common.loading.pleaseWait');   // 与桌面语义相同 → 复用已有 key
```

「深色模式」「文档大纲」「空目录」「无法加载文件」「桌面版」「关于」等逐条处理；与桌面语义相同的复用桌面/common key，mobile 独有的用 `web.mobile.*`。

- [ ] **Step 2: 校验 + 手工验证 + commit**

```bash
bash scripts/check-i18n.sh packages/core/src/web/mobile
pnpm build && pnpm test && pnpm dev:restart
```

手机（或浏览器窄屏 + `/mobile/`）zh / en 双语走查两页。

```bash
git add packages/core/src/web/mobile packages/core/src/i18n/locales
git commit -m "refactor(web): externalize mobile pages strings to i18n

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 17: 英文措辞统一审查 + key parity

**Files:**
- Modify: `packages/core/src/i18n/locales/en-US.json`（仅措辞修订）
- Modify: `packages/core/src/i18n/locales/zh-CN.json`（如发现遗漏）

**Interfaces:**
- Consumes: 全部已提取词条
- Produces: 终版双语词条表

- [ ] **Step 1: key parity 校验**

```bash
node scripts/check-i18n-keys.mjs
```

Expected: `✅ i18n key parity OK (N keys)`，N 应在 400-600 区间；若明显偏低，对照 Task 3 保存的基线清单排查漏提取的文件。

- [ ] **Step 2: en-US 全文措辞审查**

通读 `en-US.json`，统一术语：Approve/Reject（审批）、Register（注册项目）、Sensitive file（敏感文件）、Pending approval（待审批）、Sign in（登录）；按钮用祈使句首字母大写（Save changes / Discard changes）；错误消息句尾不加句号跟随现有英文软件惯例可统一为不加。占位符名与 zh 侧逐条核对一致（`{n}` / `{name}` / `{taskId}`）。

- [ ] **Step 3: Commit**

```bash
pnpm build && pnpm test
git add packages/core/src/i18n/locales
git commit -m "style(i18n): unify en-US terminology and verify key parity

Co-Authored-By: xyy277 <907507646@qq.com>"
```

---

### Task 18: 全仓扫描归零 + 纳入 CI

**Files:**
- Modify: `.github/workflows/ci.yml`（加 check:i18n step）
- Modify: `CLAUDE.md`（「提交前 CI 预检」链加入 `pnpm check:i18n`）
- Modify: `docs/planning/implementation-status.md`（如需登记本次改造）

**Interfaces:**
- Consumes: Task 3 的 `pnpm check:i18n`
- Produces: CI 门禁，杜绝未来硬编码中文回归

- [ ] **Step 1: 全仓扫描必须归零**

```bash
pnpm check:i18n
```

Expected: `✅ check-i18n passed`（exit 0）。若有残留：残留项即遗漏文件，回到通用流程提取后重跑，直到归零。

- [ ] **Step 2: ci.yml 加步骤**

在现有 lint/test 步骤旁加（对齐该文件既有步骤写法）：

```yaml
      - name: Check i18n coverage
        run: pnpm check:i18n
```

- [ ] **Step 3: CLAUDE.md 预检链更新**

「提交前 CI 预检」一行改为：

```markdown
- **提交前 CI 预检** — 每次提交前运行 `pnpm format:check && pnpm lint && pnpm check:i18n && pnpm build && pnpm test`，确保 CI 全绿
```

- [ ] **Step 4: 终验 + commit**

```bash
pnpm format:check && pnpm lint && pnpm check:i18n && pnpm build && pnpm test
git add .github/workflows/ci.yml CLAUDE.md docs
git commit -m "chore(ci): enforce i18n coverage check in CI pipeline

Co-Authored-By: xyy277 <907507646@qq.com>"
```

最终人工验收清单：

| 场景 | 期望 |
|---|---|
| 浏览器 zh → 全站中文；切 en → 全站英文（含 toast/confirm/空状态） | ✅ |
| `localStorage.doc77_lang` 覆盖优先于全局设置 | ✅ |
| `LANG=en_US.UTF-8 doc77 start`（config 为空）→ CLI 英文输出 | ✅ |
| 全局语言 en 时 API 错误英文 + `code` 字段 | ✅ |
| 全局语言 en 重启后 MCP tool descriptions 英文 | ✅ |
| AI 对话在 en 下用英文回复（system prompt 生效） | ✅ |
| 放置 `~/.doc77/locales/ja-JP.json`（拷贝 en-US.json 改 `_meta` 与几条词条）重启 → 设置下拉出现日本語，缺失词条回退英文 | ✅ |
