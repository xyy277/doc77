import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * Register all MCP prompts on the given server.
 *
 * Prompts are MCP protocol primitives for exposing reusable prompt templates.
 * This module is a placeholder — prompts will be implemented in Phase 3.
 *
 * @param server - The McpServer instance to register prompts on.
 */
export function registerPrompts(server: McpServer): void {
  // No prompts registered yet.
  // Phase 3 will populate this with reusable prompt templates.
  void server;
}
