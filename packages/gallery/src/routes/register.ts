import type { GalleryOptions } from '../types.js';

/**
 * Register gallery routes on the Express app.
 * Stub — implementation coming in later tasks.
 */
export function registerGalleryRoutes(options: GalleryOptions): void {
  // TODO: implement gallery routes
}
